# Array ADT

1. Space
2. Size
3. Length (No. of elements)

## Operations:

-   Display()
-   Add(value) or Append(value)
-   Insert(index, value)
-   Delete(index)
-   Search(value)
-   Get(index)
-   Set(index, value)
-   Max()
-   Min()
-   Reverse()
-   Rotate(times)
